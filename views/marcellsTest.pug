extends layout

block layout-content
    
    label( for="productName" ) Enter the name of the product 
    input#productName(type="text", name="productName")
    script.
      
      function onAddToBasketClick( id ) {
        fun = #{addToBasket}
        fun(id)
      }
    //       p asdasd
    //       if n == 3
    //         p hello from 3
    - var n = 0
    // each product in products
    //   div.center
    //     each product2 in product
    //         h1= product2.name
    //         button(onclick='onAddToBasketClick( ' + product2.id + ' )') Add to basket
    div.divForheader
    each list in products
      div.center
        each product in list
          div.center
            .imageClass
              p= product.id
              // h1= product.name
              // p= product.id
              - const p = product.photoUrls
              console.log( p )
              img.picView(src= "p".replace("p", product.photoUrls)  alt='login' style='width:100px; height:100px;')
              // img.picView(src='/images/fried-rice.jpg' alt='login' style='width:120px; height:120px;')
              .center
                button.basketB(onclick='onAddToBasketClick( ' + product.id + ' )') Add to basket
      